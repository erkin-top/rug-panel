# 🚀 Быстрый старт Rug-Panel

> Минимальная инструкция для запуска за 5 минут

## Шаг 1: Подготовка сервера

Вам нужен Linux сервер с:
- Ubuntu 20.04+ / Debian 11+ (или любой современный Linux)
- Docker установлен
- Публичный IP адрес

```bash
# Проверка версии ядра (должно быть >= 5.6)
uname -r

# Установка Docker (если нет)
curl -fsSL https://get.docker.com | sh
```

## Шаг 2: Запуск Rug-Panel

### Вариант A: Из исходников

```bash
git clone https://github.com/rugpanel/rug-panel.git
cd rug-panel
cp .env.example .env

# Сгенерируйте SECRET_KEY
python3 -c "import secrets; print(secrets.token_urlsafe(32))"

# Вставьте сгенерированный ключ в .env файл
nano .env  # Найдите SECRET_KEY= и вставьте ключ

# Запуск
docker compose up -d
```

### Вариант B: Готовый образ

```bash
mkdir rug-panel && cd rug-panel
mkdir data

# Создайте docker-compose.yml
cat > docker-compose.yml << 'EOF'
services:
  rug-panel:
    image: rugpanel/rug-panel:latest
    container_name: rug-panel
    restart: unless-stopped
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    ports:
      - "8000:8000/tcp"
      - "51820:51820/udp"
    environment:
      - SECRET_KEY=ЗАМЕНИТЕ_МЕНЯ_НА_СЛУЧАЙНЫЙ_КЛЮЧ
      - LANGUAGE=ru
    volumes:
      - ./data:/app/data
      - /lib/modules:/lib/modules:ro
    sysctls:
      - net.ipv4.ip_forward=1
      - net.ipv4.conf.all.src_valid_mark=1
EOF

# Сгенерируйте ключ и обновите docker-compose.yml
python3 -c "import secrets; print('Замените SECRET_KEY на:', secrets.token_urlsafe(32))"
nano docker-compose.yml  # Замените SECRET_KEY

# Запуск
docker compose up -d
```

## Шаг 3: Первый вход

1. Откройте браузер: `http://ВАШ_IP:8000`
2. Войдите: `admin` / `admin`
3. **Сразу смените пароль!** → Профиль → Сменить пароль

## Шаг 4: Настройка WireGuard

1. Перейдите в "Сервер" → "Настройки"
2. Убедитесь что **Server Endpoint** = ваш публичный IP
3. Если нет - введите и сохраните

## Шаг 5: Создание VPN клиента

1. Dashboard → **"Добавить клиента"**
2. Заполните:
   - Имя: например `iPhone-Ivan`
   - Остальное оставьте по умолчанию
3. **Создать**
4. Выберите способ подключения:
   - **QR-код** → сканируйте в WireGuard приложении
   - **Скачать конфиг** → импортируйте файл

## Шаг 6: Подключение устройства

### Мобильный телефон (iOS/Android)

1. Установите [WireGuard](https://www.wireguard.com/install/)
2. Откройте приложение → "Добавить туннель"
3. Отсканируйте QR-код из панели
4. Включите туннель

### Компьютер (Windows/macOS/Linux)

1. Установите [WireGuard](https://www.wireguard.com/install/)
2. Скачайте конфиг из панели
3. Импортируйте файл в приложение
4. Активируйте туннель

## ✅ Готово!

Теперь у вас работает личный VPN сервер!

---

## 🆘 Что-то не работает?

### WireGuard не запускается

```bash
# Проверьте логи
docker compose logs -f

# Проверьте модуль ядра
sudo modprobe wireguard
```

### Клиент не подключается

- Проверьте что порт 51820/UDP открыт в firewall
- Убедитесь что Server Endpoint правильный
- Проверьте логи: `docker compose logs -f`

### Панель недоступна

```bash
# Проверьте статус контейнера
docker compose ps

# Перезапуск
docker compose restart
```

---

## 📚 Подробная документация

- [README.md](README.md) - полная документация
- [DEPLOYMENT.md](DEPLOYMENT.md) - детальное руководство по развёртыванию
- [GitHub Issues](https://github.com/rugpanel/rug-panel/issues) - поддержка

---

**Поздравляем с запуском! 🎉**
